services:
  - type: web
    name: financial-news-sentiment-api
    runtime: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      uvicorn src.api.app:app --host 0.0.0.0 --port $PORT